#include "ModelKeyFrame.h"

namespace Graphics {

    ModelKeyFrame ModelKeyFrame::lerp(const ModelKeyFrame& a, const ModelKeyFrame& b, float x) {
        ModelKeyFrame result;
		
        // todo: ...interpolate result...
        
		return result;
    }

}